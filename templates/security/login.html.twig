{% extends 'base.html.twig' %}

{% block title %}{{ 'app.security.login_title'|trans }}{% endblock %}

{% block body %}
<div class="mx-auto max-w-md">
    <div class="rounded-2xl border border-cyan-200/15 bg-ink-900/40 p-8 shadow-glow">
        <h1 class="font-display text-xl tracking-wide text-slate-100 mb-6">{{ 'app.security.login'|trans }}</h1>

        {% if error %}
            <div class="mb-4 rounded-lg border border-red-400/30 bg-red-500/10 px-4 py-3 text-sm text-red-200">
                {{ error.messageKey|trans(error.messageData, 'security') }}
                {% if error.messageKey == 'Invalid CSRF token.' %}
                    <p class="mt-2 text-xs text-red-300">{{ 'app.security.reload_csrf'|trans }}</p>
                {% endif %}
            </div>
        {% endif %}

        <form method="post" action="{{ path('app_login') }}" class="space-y-5">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <div>
                <label for="username" class="mb-1.5 block text-sm font-medium text-slate-300">{{ 'app.security.email'|trans }}</label>
                <input type="email"
                       name="_username"
                       id="username"
                       value="{{ last_username }}"
                       required
                       autofocus
                       autocomplete="email"
                       class="w-full rounded-lg border border-cyan-200/20 bg-ink-800/60 px-4 py-2.5 text-slate-100 placeholder-slate-500 focus:border-neon-400/50 focus:outline-none focus:ring-2 focus:ring-neon-400/20"
                       placeholder="{{ 'app.security.email_placeholder'|trans }}">
            </div>

            <div>
                <label for="password" class="mb-1.5 block text-sm font-medium text-slate-300">{{ 'app.security.password'|trans }}</label>
                <input type="password"
                       name="_password"
                       id="password"
                       required
                       autocomplete="current-password"
                       class="w-full rounded-lg border border-cyan-200/20 bg-ink-800/60 px-4 py-2.5 text-slate-100 placeholder-slate-500 focus:border-neon-400/50 focus:outline-none focus:ring-2 focus:ring-neon-400/20"
                       placeholder="••••••••">
            </div>

            <button type="submit"
                    class="w-full rounded-lg bg-gradient-to-r from-neon-500/90 to-cyan-500/90 px-4 py-3 font-medium text-ink-950 shadow-glow transition hover:shadow-glowStrong focus:outline-none focus:ring-2 focus:ring-neon-400/40">
                {{ 'app.security.submit_login'|trans }}
            </button>

            <p class="text-center text-sm text-slate-400">
                <a href="{{ path('app_request_reset_password') }}" class="font-medium text-cyan-300 hover:text-cyan-200 underline underline-offset-2">{{ 'app.security.forgot_password'|trans }}</a>
            </p>

            <p class="text-center text-sm text-slate-400">
                {{ 'app.security.no_account'|trans }}
                <a href="{{ path('app_register') }}" class="font-medium text-cyan-300 hover:text-cyan-200 underline underline-offset-2">{{ 'app.security.register'|trans }}</a>
            </p>
        </form>
    </div>
</div>
{% endblock %}
